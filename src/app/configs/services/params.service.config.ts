import {privateParams} from '../../../privateparams';

export const ParamsServiceConfig = {
  people: {
    apiUrl: privateParams.links.origamiPeoples,
    embeddedListKey: 'peoples',
    formItemKey: 'people',
  }
};
